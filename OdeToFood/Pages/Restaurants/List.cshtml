@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
}

<h1>
    Restaurant
</h1>
<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-btn">
                <button class="btn btn-default">
                    <i class="fas fa-search"></i>
                </button>
            </span>

        </div>
    </div>

</form>

@foreach (var Restaurant in Model.Restaurants)
{
    <partial name="_Summery" model="Restaurant" />
}
    <a class="btn btn-primary" asp-page="./Edit">
        Add New
    </a>
    @Model.Message
    @*<table class="table">

            @foreach (var item in Model.Restaurants)
            {
            <tr>
                <td>@item.Name</td>
                <td>@item.Location</td>
                <td>@item.Cuisine</td>
                <td>
                    <a class="btn btn-lg" asp-page="./Details" asp-route-restaurantId="@item.Id">
                        <i class="glyphicon glyphicon-zoom-in"></i>
                    </a>
                </td>
                <td>
                    <a class="btn btn-lg" asp-page="./Edit" asp-route-restaurantId="@item.Id">
                        <i class="glyphicon glyphicon-Edit"></i>
                    </a>
                </td>
                <td>
                    <a class="btn btn-lg" asp-page="./Delete" asp-route-restaurantId="@item.Id">
                        <i class="glyphicon glyphicon-trash"></i>
                    </a>
                </td>

            </tr>
            }
        </table>*@
